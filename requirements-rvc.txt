# Isolated RVC worker venv — DO NOT install into the main VoiceForge venv.
# fairseq + numpy<2 conflict with coqui-tts / numpy 2.x in the primary stack.
#
# Install (GPU host example):
#   python3.11 -m venv /opt/rvc-venv
#   /opt/rvc-venv/bin/pip install torch==2.1.1 torchaudio==2.1.1 \
#       --index-url https://download.pytorch.org/whl/cu121
#   /opt/rvc-venv/bin/pip install -r requirements-rvc.txt
#   /opt/rvc-venv/bin/pip install git+https://github.com/nakshatra-garg/rvc-no-gui.git
#
# Point VoiceForge at the worker:
#   export VOICEFORGE_RVC_PYTHON=/opt/rvc-venv/bin/python

rvc-python==0.1.5
numpy==1.23.5
omegaconf==2.0.6
fairseq==0.12.2
faiss-cpu==1.7.4
librosa>=0.10.0
pyworld>=0.3.4
praat-parselmouth>=0.4.3
ffmpeg-python>=0.2.0
loguru>=0.7.0
av>=11.0.0
torchcrepe>=0.0.22
requests>=2.31.0
soundfile>=0.12.0
tqdm>=4.66.0
